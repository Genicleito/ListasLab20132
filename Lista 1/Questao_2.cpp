#include <stdio.h>

int main(){
	int x;
	scanf("%d", &x);
	printf("%d", x);
						/*Resposta: O programa lê um valor digitado pelo usuário e armazena na variável x.
						Logo em seguida, o programa mostra o valor digitado para 
						que o usuario possa vizualizá-lo na tela.*/				
	return 0;	
}
